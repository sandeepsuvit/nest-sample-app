
/**
 * Payload data for authenticated user
 *
 * @export
 * @interface AuthUserPayload
 */
export interface AuthUserPayload {
    _id: string;
    firstName: string;
    lastName: string;
    email: string;
}
